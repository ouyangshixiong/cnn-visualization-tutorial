# Core frameworks - 虚拟环境专用版本
# 确保在venv_linux环境中使用

# PyTorch (CPU版本，可根据需要改为GPU版本)
torch>=2.1.0
torchvision>=0.16.0

# PaddlePaddle (CPU版本)
paddlepaddle>=2.5.0
paddleclas>=2.5.0

# 可视化库
matplotlib>=3.8.0
seaborn>=0.13.0
plotly>=5.17.0
opencv-python>=4.8.0
Pillow>=10.0.0

# 科学计算
numpy>=1.25.0
scipy>=1.11.0
scikit-learn>=1.3.0

# 工具库
tqdm>=4.66.0
python-dotenv>=1.0.0

# 图像处理
imageio>=2.31.0
tifffile>=2023.8.0

# 注意：GPU版本需要单独安装
# GPU版本PyTorch: pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118
# GPU版本PaddlePaddle: pip install paddlepaddle-gpu
EOF < /dev/null